1. Working with dfField
1.1 Declare a field with its DOF available
dgField<scalar> pField
(
    IOobject
    (
        "p",
        runTime.timeName(),
        mesh,
        IOobject::MUST_READ,
        IOobject::AUTO_WRITE
    ),
    dgMesh,
    true
);

1.2 Declare a field with no values, as a container
dgField<scalar> TestField
(
    IOobject
    (
        "TestField",
        runTime.timeName(),
        mesh,
        IOobject::NO_READ,
        IOobject::AUTO_WRITE
    ),
    dgMesh,
    false
);

1.3 Access to a GaussField of a cell
Info << TField.gaussFields()[0] << endl; // 0 is cell ID

1.4 Check the availability of dgField in registry
Info<< "\n--- Checking registered objects in mesh.thisDb() ---" << nl;
const objectRegistry& db = mesh.thisDb();
forAllConstIter(objectRegistry, db, iter)
{
Info<< "Registered object: " << iter()->name()
    << " (type: " << iter()->type() << ")" << nl;
}
